name: supervisor_proximity
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+2

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  google_fonts: ^6.2.1
  camera: ^0.11.1
  pin_code_fields: ^8.0.1
  lottie: ^3.3.1
  avatar_glow: ^3.0.1
  firebase_core: ^4.10.0
  firebase_auth: ^6.5.2
  cloud_firestore: ^6.5.0
  image_picker: ^1.1.2
  shared_preferences: ^2.3.2
  http: ^1.6.0
  flutter_map: ^8.3.0
  latlong2: ^0.9.1
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  package_info_plus: ^10.1.0
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/google_fonts/
    - assets/images/
